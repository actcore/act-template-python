[project]
name = "{{ project_name }}"
version = "0.1.0"
description = "{{ description }}"
requires-python = ">=3.14"
dependencies = [
    "act-sdk>=0.1.0",
]

[dependency-groups]
dev = [
    "componentize-py>=0.21.0",
]
